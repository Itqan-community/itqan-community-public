# Itqan Reactions

Emoji reactions on posts. Separate from vote ranking (`itqan-discussions`).

- One active reaction type per user per post (toggle off to clear)
- Unicode emoji (no CDN)
- Compact chips in the post action bar

## Dev

```bash
cd packages/itqan-reactions/js && npm install && npm run build
```
